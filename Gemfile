source 'https://rubygems.org'


gem 'bootstrap-sass',       '~> 3.2.0.2'
#gem 'bootstrap-social-rails'
gem 'bootstrap-social-rails', '~> 4.8.0'
gem 'font-awesome-rails'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'




# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
#gem 'rails', '4.0.0'
#gem 'rails', '4.0.1'
gem 'rails', '4.2.1'

# Use MySQL as the database for Active Record
gem 'mysql2'


# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
#gem 'coffee-rails', '~> 4.0.0'
#gem 'coffee-rails', '~> 4.0.1'
gem 'coffee-rails', '~> 4.1.0'

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Rails 4 removes nondigest asset creation during precompile (e.g. application.css as well as application-DIGESTHASH.css). This gem re-instates those, so you can use one URL for the current version of assets for external resources (e.g. Dreamweaver referencing a CSS file).
#gem 'sprockets-redirect'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

# local additions
gem 'therubyracer'
#gem 'will_paginate'
#gem 'multi_json'
gem 'net-ldap', :require => 'net/ldap'
#gem 'sslrequirement'
#gem 'paperclip', '>= 3.5.1'
gem 'paperclip', '>= 4.2.1'
gem 'rails-observers'

